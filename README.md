# pageturner

> A basic pagination utility for websites

## Usage

```sh
$ npm install pageturner --save
```

```html
<script>
  var pageTurner = new PageTurner({
    "animate": "slow",
    "itemClass": "page-item",
  });
  pageTurner.run();
</script>
```
